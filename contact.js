const grab = (element) => document.querySelector(element);

const submitBtn = grab('#contact-submit-btn');

// submitBtn.addEventListener('click', (e) => {
//     e.preventDefault();
//     console.log('Click!')
// })